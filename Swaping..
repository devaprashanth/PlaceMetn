#include<stdio.h>
int main()
{
    int a=7,b=5,c;
    printf("BEFORE SWAPING...\n");
    printf("a=%d,b=%d\n",a,b);
   b=c;
    printf("AFTER SWAPING...\n");
    printf("a=%d,b=%d",a,b);
    return 0;

}
